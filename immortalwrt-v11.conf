# setRepoURL: https://github.com/immortalwrt/immortalwrt
# setRepoBRANCH: master
# setTAG: SNAPSHOT
# setIP: 192.168.31.1
# setMASK: 192.168.31.255
# setCONFIG: v11
# setUserPasswd: root()root()
# setTimeZone: Asia/Shanghai
# setCacheToolchain: false
# setUploadBin: true
# setUploadFirmware: true
# setUploadRelease: true
#
# r32360-dad5350c6f local
# Date: 2025/02/05 14:00:00
#
# make menuconfig
# 	Target System
# 		MediaTek Ralink MIPS
# 	Subtarget
# 		MT7621 based boards
# 	Target Profile
# 		Xiaomi Mi Router 3 Pro
# 	Target Images
# 		[*] ramdisk
# 		[*] squashfs
# 退出重进
# 	LuCI
# 		1.Collections
# 			[*] luci
# 		2.Modules
# 			Translations
# 				[*] Chinese Simplified (zh_Hans)
# 			[*] luci-compat
# 			[*] luci-lua-runtime
# 			[*] luci-mod-dashbard
# 		3.Applications
# 			[*] luci-app-adguardhome
# 			[*] luci-app-argon-config
# 			[*] luci-app-diskman
# 			[*] luci-app-firewall
# 			[*] luci-app-samba4
# 			[*] luci-app-smartdns
# 			[*] luci-app-statistics
# 			[*] luci-app-syncthing
# 			[*] luci-app-v2raya
# 		4.Themes
# 			[*] luci-theme-argon
# 			[*] luci-theme-bootstrap
# 		[*] default-settings
# 		[*] default-settings-chn
# 退出重进
# 	Utilities
# 		Editors
# 			[*] vim-full
# 		Filesystem
# 			[*] f2fs-tools
# 			[*] ntfs-3g
# 			[*] ntfs-3g-utils		【ntfsfix需要】
# 			[M] ntfs3-mount
# 		Time Zone info
# 			[*] zoneinfo-asia
# 		[*] collectd
# 			[*] collectd-mod-cpu
# 			[*] collectd-mod-disk
# 			[*] collectd-mod-interface
# 			[*] collectd-mod-iwinfo
# 			[*] collectd-mod-load
# 			[*] collectd-mod-memory
# 			[*] collectd-mod-network
# 			[*] collectd-mod-ping
# 			[*] collectd-mod-processes
# 			[*] collectd-mod-rrdtool
# 			[*] collectd-mod-sensors
# 			[*] collectd-mod-uptime
# 		[*] dmesg		【有颜色】
# 		[*] hwinfo
# 		[*] lm-sensors
# 		[*] lm-sensors-detect
# 		[*] logrotate
# 		[*] usbutils
# 退出重进
# 	Network
# 		Firewall
# 			[*] ip6tables-nft
# 			[*] iptables-nft
# 		IP Address and Names
# 			[*] bind-dig
# 			[*] bind-nslookup
# 			[*] dnslookup
# 		Time Synchronization
# 			[*] ntpdate
# 		[*] adguardhome
# 		[*] ifstat
# 		[*] iftop
# 		[*] iperf
# 			[*] Enable multicast support
# 		[*] iperf3
# 		[*] ipset
# 		[ ] iw
# 		[*] iw-full
# 		[*] speedtest-netperf
# 		[*] tcpdump
# 		[*] tcping
# 		[*] tcpping
# 		[*] v2ray-core
# 			[*] v2ray-extra
# 		[*] xray-core
# 退出重进
# 	Extra packages
# 		[ ] automount		【与ntfs3-mount冲突】
# 		[*] autosamba
# 	Kernel modules
# 		Filesystems
# 			[*] kmod-fs-exfat
# 			[*] kmod-fs-ext4
# 			[*] kmod-fs-f2fs
# 			[*] kmod-fs-ntfs
# 			[*] kmod-fs-ntfs3
# 			[*] kmod-fs-vfat
# 		Native Language Support
# 			[*] kmod-nls-base
# 			[*] kmod-nls-cp437
# 			[*] kmod-nls-cp936
# 			[*] kmod-nls-iso8859-1
# 			[*] kmod-nls-utf8
# 		Network Support
# 			[*] kmod-tcp-bbr
# 			[*] kmod-tcp-hybla
# 			[*] kmod-tcp-scalable
# 			[*] kmod-tls
# 			[*] kmod-trelay
# 			[*] kmod-tun
# 			[*] kmod-wireguard
# 		USB Support
# 			[*] kmod-usb-ohci
# 			[*] kmod-usb-ohci-pci
# 			[*] kmod-usb-storage
# 			[*] kmod-usb-storage-extras
# 			[*] kmod-usb-storage-uas
# 			[*] kmod-usb-uhci
# 			[*] kmod-usb2
# 			[*] kmod-usb2-pci
# 			[*] kmod-usb3
# 			[*] kmod-usbmon
# 退出重进
# 	Base system
# 		[*] block-mount
# 		[*] blockd
# 		[*] bridge
# 		[*] Customize busybox options
# 			Coreutils
# 				[*] paste
# 				[*] printenv
# 				[*] usleep
# 				[*] base64
# 				[*] whoami
# 			Linux Ext2 FS Progs
# 				[*] fsck
# 			Linux Module Utilities
# 				[*] lsmod
# 				[*] modinfo
# 			Linux System Utilities
# 				[*]  blkid
# 					[*] Print filesystem type
# 				[*] blockdev
# 				[*] dmesg			【选下面有颜色的】
# 				[*] fdisk
# 				[*] findfs
# 				[*] fdflush
# 				[*] getopt
# 				[*] hd
# 				[*] lspci
# 				[ ] lsusb			【usbutils包含】
# 				[*] more
# 				[*] mount
# 					[*] Support -f (fake mount)
# 					[*] Support -v (verbose)
# 					[*] Support specifying devices by label or UUID
# 				Filesystem/Volume identification
# 					[*] exFAT filesystem
# 					[*] Ext filesystem
# 					[*] f2fs filesystem
# 					[*] fat filesystem
# 					[*] ntfs filesystem
# 			Miscellaneous Utilities
# 				[*] hdparm
# 				[*] inotifyd
# 				[*] man
# 				[*] tree
# 			Networking Utilities
# 				[ ] ifup					【netifd包含】
# 				[ ] ifdown				【netifd包含】
# 					[ ] Support IPv4
# 					[ ] Support IPv6
# 				[*] ipaddr
# 				[*] iproute
# 				[*] telnet
# 					[*] Pass TERM type to remote host
# 					[*] Pass USER type to remote host
# 					[*] Enable window size autodetection
# 				[*] tftp
# 					[*] Enable progress bar
# 				[*] wget
# 					[*] Enable long options
# 					[*] Enable progress bar
# 					[*] Enable FTP protocol
# 					[*] Enable HTTP authentication
# 					[*] Enable timeout option -T SEC
# 					[*] Support HTTPS using internal TLS code
# 					[*] Try to connect to HTTPS using openssl
# 				[*] whois
# 			Process Utilities
# 				[*] Support thread display in ps/pstree/top
# 				[*] lsof
# 				[*] pkill
# 				[*] ps
# 					[*] Enable wide output (-w)
# 					[*] Enable long output (-l)
# 				[*] pstree
# 				[*] top
# 					[*] Accept keyboard commands
# 				[*] watch
# 			System Logging Utilities
# 				[*] logread
# 					[*] Double buffering
# 				[*] syslogd
# 					[*] Support syslog.conf
# 		[ ] dnsmasq
# 		[*] dnsmasq-full
# 			[*] Build with IPset support
# 		[*] wireless-tools
# 		[*] zram-swap
#
# Automatically generated file; DO NOT EDIT.
# ImmortalWrt Configuration
#