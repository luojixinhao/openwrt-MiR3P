# setRepoURL: https://github.com/immortalwrt/immortalwrt
# setRepoBRANCH: master
# setTAG: SNAPSHOT
# setIP: 192.168.31.1
# setMASK: 192.168.31.255
# setCONFIG: v8
# setUserPasswd: root()root()
# setTimeZone: Asia/Shanghai
# setCacheToolchain: false
# setUploadBin: true
# setUploadFirmware: true
# setUploadRelease: true
#
# r27160-b72c4b5386 NEW
# Date: 2024-08-16 20:40:34 +0200
#
# make menuconfig
# 	Target System
# 		MediaTek Ralink MIPS
# 	Subtarget
# 		MT7621 based boards
# 	Target Profile
# 		Xiaomi Mi Router 3 Pro
# 	Target Images

# 	[*] Enable experimental features by default (NEW)

# 	Global build settings
# 		[*] Show packages that require graphics support (local or remote) (NEW)
# 		Kernel build options
# 			[*] Compile the kernel with task resource/io statistics and accounting
# 			[ ] Compile the kernel with SCSI generic v4 support for any block device (NEW)
# 			[*] Enable IPv4 multicast routing (NEW)
# 			[*] Enable IPv6 multicast routing (NEW)
# 	[ ] Advanced configuration options (for developers) (NEW)
# 	[ ] Build the OpenWrt Image Builder (NEW)
# 	[ ] Build the OpenWrt SDK (NEW)
# 	[ ] Package the OpenWrt-based Tollchain (NEW)
# 	[ ] Image configuration (NEW)
# 	Base system
# blkid
# blockdev
# fdisk
# hdparm
# whois
# more
# tar
# sed
# grep
# getopt
# dmesg
# gzip
# bzip2
# 		[*] block-mount
# 		[*] blockd
# 		[*] Customize busybox options
# 			Archival Utilities
# 				[ ] unzip
# 			Coreutils
# 				[*] dd
# 					[*] Enable signal handling for status reporting
# 						[*] Enable the third status line upon signal
# 				[*] df
# 					[*] Enable -a, -i, -B
# 				[*] paste
# 				[*] printenv
# 				[*] whoami
# 			Editors
# 				[*] diff
# 			Finding Utilities
# 				[*] Enable -mtime: modification time matching
# 					[*] Enable -atime: access time matching
# 					[*] Enable -ctime: status change timestamp matching
# 				[*] Enable -mmin: modification time matching by minutes
# 					[*] Enable -amin: access time matching by minutes
# 					[*] Enable -cmin: status change timestamp matching by minutes
# 			Init Utilities
# 				[ ] init
# 				[ ] linuxrc: support running init from initrd (not initramfs)
# 					[ ] Support reading an inittab file
# 					[ ] Enable init to write to syslog

# 			Login/Password Management Utilities
# 				[*] addgroup
# 					[*] Support adding users to groups
# 				[*] adduser
# 				[*] deluser
# 				[*] delgroup
# 					[*] Support removing users from groups
# 			Linux Ext2 FS Progs
# 				[ ] fsck
# 			Linux Module Utilities
# 				[ ] lsmod
# 			Linux System Utilities
# 				[ ]  blkid
# 					[ ] Print filesystem type
# 				[ ] fdisk
# 				[ ] fsck.minix
# 				[*] getopt
# 				[*] hd
# 				[ ] lspci
# 				[ ] lsusb
# 				[ ] mkdosfs
# 				[ ] mkfs.vfat
# 				[ ] more
# 				[*] mount
# 					[*] Support -f (fake mount)
# 					[*] Support -v (verbose)
# 					[*] Support specifying devices by label or UUID
# 				Filesystem/Volume identification
# 					[*] exFAT filesystem
# 					[*] Ext filesystem
# 					[*] f2fs filesystem
# 					[*] fat filesystem
# 					[*] ntfs filesystem
# 			Networking Utilities
# 				[ ] ifup
# 				[ ] ifdown
# 					[ ] Support IPv4
# 					[ ] Support IPv6
# 				[*] iproute
# 				[*] telnet
# 					[*] Pass TERM type to remote host
# 					[*] Pass USER type to remote host
# 				[ ] wget
# 					[ ] Support HTTPS using internal TLS code
# 					[ ] Try to connect to HTTPS using openssl
# 				[ ] whois
# 			Process Utilities
# 				[*] Support thread display in ps/pstree/top
# 				[*] pstree
# 			Shells
# 				[*] Optimize for size instead of speed
# 			System Logging Utilities
# 				[*] logread
# 					[*] Double buffering
# 				[*] syslogd
# 					[*] Support syslog.conf
# 		[ ] dnsmasq
# 		[*] dnsmasq-full
# 			[*] Build with IPset support
# 		[*] wireless-tools
# 	Administration
# 		Zabbix
# 			[ ] zabbix-extra-mac80211
# 			[ ] zabbix-extra-network
# 			[ ] zabbix-extra-wifi
# 		[ ] syslog-ng
# 	Boot Loaders

# 	Development

# 	Extra packages

# 	Firmware

# 	Fonts

# 	Kernel modules
# 		Block Devices
# 			[*] kmod-block2mtd
# 			[*] kmod-nvme
# 			[*] kmod-scsi-core
# 			[ ] kmod-scsi-generic
# 		Filesystems
# 			[*] kmod-fs-configfs
# 			[*] kmod-fs-exfat
# 			[*] kmod-fs-ext4
# 			[*] kmod-fs-f2fs
# 			[*] kmod-fs-msdos
# 			[*] kmod-fs-ntfs
# 			[*] kmod-fs-ntfs3
# 			[*] kmod-fs-squashfs
# 		Native Language Support
# 			[*] kmod-nls-base
# 			[*] kmod-nls-cp437
# 			[*] kmod-nls-cp936
# 			[*] kmod-nls-iso8859-1
# 			[*] kmod-nls-utf8
# 		Netfilter Extensions
# 			[*] kmod-nft-arp
# 			[*] kmod-nft-bridge
# 		Network Support
# 			[*] kmod-nlmon
# 			[*] kmod-sched
# 			[*] kmod-sched-core
# 			[*] kmod-tcp-bbr
# 			[*] kmod-tcp-hybla
# 			[*] kmod-tcp-scalable
# 			[*] kmod-tun
# 		Other modules
# 			[*] kmod_mmc
# 			[*] kmod_mmc-mtk
# 			[*] kmod-echo
# 			[*] kmod-mtd-rw
# 			[*] kmod-mtdoops
# 			[*] kmod-mtdram
# 			[*] kmod-mtdtests
# 		USB Support
# 			[*] kmod-usb-ohci
# 			[*] kmod-usb-ohci-pci
# 			[*] kmod-usb-storage
# 			[*] kmod-usb-storage-extras
# 			[*] kmod-usb-storage-uas
# 			[*] kmod-usb2
# 			[*] kmod-usb2-pci
# 			[*] kmod-usb3
# 		Wireless Drivers
# 			[*] kmod-cfg80211
# 				[*] Enable testmode command support
# 			[*] kmod-iwlwifi
# 			[*] kmod-mt76
# 			[*] kmod-wlcore
# 	Languages

# 	Libraries
# 			[*] libuwifi
# 	LuCI
# 		1.Collections
# 			[*] luci
# 		2.Modules
# 			Translations
# 				[*] Chinese Simplified (zh_Hans)
# 			[*] luci-lua-runtime
# 			[*] luci-mod-dashbard
# 		3.Applications
# 			[*] luci-app-adguardhome
# 			[*] luci-app-argon-config
# 			[*] luci-app-diskman
# 			[*] luci-app-dockerman
# 			[*] luci-app-firewall
# 			[*] luci-app-mosdns
# 			[*] luci-app-netdata
# 			[*] luci-app-nlbwmon
# 			[*] luci-app-openclash
# 			[*] luci-app-opkg
# 			[*] luci-app-passwall
# 				Configuration
# 					[*] Nftables Transparent Proxy
# 					[ ] Include Haproxy
# 					[*] Include Hysteria
# 					[ ] Include NaiveProxy
# 					[*] Include Shadowsocks Libev Client
# 					[*] Include ShadowsocksR Libev Client
# 					[*] Include Simple-0bfs (Shadowsocks Plugin)
# 					[*] Include Sing-Box
# 					[*] Include V2ray_Geodata
# 					[*] Include V2ray-Plugin (Shadowsocks Plugin)
# 					[*] Include Xray
# 					[*] Include Xray-Plugin (Shadowsocks Plugin)
# 			[*] luci-app-samba4
# 			[*] luci-app-smartdns
# 			[*] luci-app-statistics
# 			[*] luci-app-syncthing
# 			[*] luci-app-upnp
# 			[*] luci-app-vnstat2
# 		4.Themes
# 			[*] luci-theme-argon
# 			[*] luci-theme-bootstrap
# 		5.Protocols

# 		6.Libraries
# 			[*] luci-lib-docker

# 	Mail

# 	Multimedia

# 	Network
# 		File Transfer
# 			[*] wget-nossl
# 			[*] wget-ssl
# 		Filesystem
# 			[*] ksmbd-avahi-service
# 			[*] ksmbd-hotplug
# 			[*] ksmbd-server
# 		Firewall
# 			[*] ip6tables-nft
# 			[*] iptables-nft
# 		IP Address and Names
# 			[*] bind-dig
# 			[*] bind-nslookup
# 			[*] dnslookup
# 		Time Synchronization
# 			[*] ntpdate
# 		[*] 6in4
# 		[*] 6to4
# 		[*] adguardhome
# 		[*] ifstat
# 		[*] iftop
# 		[*] iperf
# 			[*] Enable multicast support
# 		[*] iperf3
# 		[ ] iw
# 		[*] iw-full
# 		[*] speedtest-netperf
# 		[*] tcpdump
# 		[*] v2ray-core
# 	Sound

# 	Utilities
# 		Compression
# 			[ ] gzip
# 		Disc
# 			[*] blockdev
# 			[*] fdisk
# 			[*] hdparm
# 		Editors
# 			[*] vim-full
# 		Filesystem
# 			[*] dosfstools
# 			[*] exfat-fsck
# 			[*] exfat-mkfs
# 			[*] f2fs-tools
# 			[*] ntfs-3g
# 			[*] ntfs-3g-utils
# 		Time Zone info
# 			[*] zoneinfo-asia
# 		Utilities
# 			[*] collectd
# 				[*] collectd-mod-cpu
# 				[ ] collectd-mod-cpufreq
# 				[ ] collectd-mod-df
# 				[*] collectd-mod-disk
# 				[*] collectd-mod-interface
# 				[*] collectd-mod-iwinfo
# 				[*] collectd-mod-load
# 				[*] collectd-mod-memory
# 				[*] collectd-mod-network
# 				[*] collectd-mod-ping
# 				[*] collectd-mod-processes
# 				[*] collectd-mod-rrdtool
# 				[*] collectd-mod-sensors
# 				[*] collectd-mod-uptime
# 				[ ] collectd-mod-wireless
# 			[*] cupsage
# 			[ ] dmesg
# 			[*] docker
# 			[*] docker-compose
# 			[*] dockerd
# 			[*] gawk
# 			[ ] getopt
# 			[ ] grep
# 			[*] hwinfo
# 			[*] kmod
# 			[*] lm-sensors
# 			[*] lm-sensors-detect
# 			[*] logrotate
# 			[ ] more
# 			[*] mount-utils
# 			[*] openssl-util
# 			[ ] sed
# 			[ ] tar
# 			[*] usbutils
# 			[ ] whois
# 		[ ] prometheus-node-exporter-lua
# 			[ ] prometheus-node-exporter-lua-uci_dhcp_host
# 			[ ] prometheus-node-exporter-lua-wifi
# 			[ ] prometheus-node-exporter-lua-wifi_stations
# 		[ ] prometheus-node-exporter-ucode
# 	Xorg
#